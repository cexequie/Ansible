---
- hosts: localhost
  gather_facts: no
  tasks:
  - name: Ticket Iniciar pruebas
    community.general.jira:
      uri: '{{ server }}'
      username: '{{ user }}'
      password: '{{ passwd }}'
      project: '{{ project }}'
      issue: '{{ item.issueid }}'
      operation: transition
      status: 'Iniciar pruebas'
    with_items:
      - '{{ tickets }}'
    register: issue
